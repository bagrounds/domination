{
  "image": "node:14",
  "stages": [
    "deploy"
  ],
  "pages": {
    "stage": "deploy",
    "script": ["npm run deploy"],
    "environment": "pages",
    "artifacts": {
      "paths": ["public"]
    },
    "only": ["master"]
  }
}
